# Base URL for data files (without trailing slash).
# Production (Cloudflare Pages): set to your repo's raw GitHub URL.
# Local dev: leave empty — data served from /data/ via static symlink.
PUBLIC_DATA_BASE_URL=https://raw.githubusercontent.com/qitpydev/veen-news/main
